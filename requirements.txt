# TARGET stack for M2+ (NOT needed for the M1 stdlib-only scaffold / tests).
# Install only where PyPI is reachable:  pip install -r requirements.txt
pandas>=2.0
numpy>=1.24
ccxt>=4.0
pydantic>=2.0
# ML (M5, optional): scikit-learn>=1.3 ; lightgbm>=4.0
# dev: pytest>=7.0 ; ruff ; black ; mypy
